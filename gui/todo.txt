v icon.bin as a resource
+ install and delete to be only enabled if operation could be performed
+ icons
v version resource for command-line version
- about box for gui version
v compatibility flags
- persistent list -> signature = name + status ((not) working) + flags
- limit HDD size to 128Gigs
v installation from an IML
- ELF started via ps2link to make installation thru host:
- if IML-linked files are not found check relative the .IML file path
v CLI option to return startup file and volume label
- win9x support via ASPI?
